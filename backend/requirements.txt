# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # required for FastAPI file uploads

# ── Deep learning ─────────────────────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0

# ── Grad-CAM ──────────────────────────────────────────────────────────────────
torchcam>=0.4.0

# ── Lung segmentation: TorchXRayVision PSPNet (replaces broken HuggingFace U-Net)
# Maintained by mlmed/torchxrayvision (Cohen et al., MIDL 2022).
# Weights hosted on GitHub Releases — stable, institutionally maintained.
# PSPNet trained on NIH ChestX-Det (annotated by 3 board-certified radiologists).
torchxrayvision>=1.0.1

# ── Morphological mask post-processing (fill + convex hull per lobe) ──────────
scipy>=1.13.0
scikit-image>=0.22.0             # convex_hull_image for hilar gap closure

# ── Image I/O ─────────────────────────────────────────────────────────────────
Pillow>=10.3.0
numpy>=1.26.0

# ── DICOM support (optional but recommended) ──────────────────────────────────
pydicom>=2.4.0

# ── Removed dependencies ──────────────────────────────────────────────────────
# segmentation-models-pytorch  — was used only for the broken U-Net segmenter;
#   no longer needed.  Remove from your environment: pip uninstall segmentation-models-pytorch
# huggingface_hub              — was used only to download the now-deleted checkpoint;
#   no longer needed.  Remove: pip uninstall huggingface_hub